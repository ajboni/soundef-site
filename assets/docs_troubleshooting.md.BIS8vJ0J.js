import{_ as o,o as a,c as d,a2 as s}from"./chunks/framework.Ck6QqhZl.js";const p=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"docs/troubleshooting.md","filePath":"docs/troubleshooting.md"}'),r={name:"docs/troubleshooting.md"};function i(t,e,n,l,c,h){return a(),d("div",null,[...e[0]||(e[0]=[s('<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h1><p>Task-oriented fixes for common problems. Every symptom maps to a diagnostic code or a behavior described in the <a href="./spec">Spec</a>. For the full code list, see <a href="./spec#diagnostics">Diagnostics</a>.</p><h2 id="a-layer-is-silent" tabindex="-1">A layer is silent <a class="header-anchor" href="#a-layer-is-silent" aria-label="Permalink to &quot;A layer is silent&quot;">​</a></h2><ul><li><strong>The layer was skipped.</strong> If the layer has <code>probability</code> below <code>1</code>, its per-trigger roll may have failed - the resolved layer is marked <code>skipped: true</code> and does not play. Try a different <code>seed</code>, or raise <code>probability</code>. Forge shows skipped layers as dashed ghosts on the timeline.</li><li><strong>The source resolved to zero files.</strong> A glob that matches nothing emits <code>unresolved-source</code> (warning) and, if a layer ends up with no sources, <code>no-sources</code> (error, blocks the plan). Check the basename glob against your <code>sounds/</code> folder.</li><li><strong>Gain is very low.</strong> A large negative <code>gain</code> (e.g. <code>-20dB</code>) is nearly inaudible. Confirm the value and unit.</li></ul><h2 id="a-glob-matches-0-files-unresolved-source" tabindex="-1">A glob matches 0 files (<code>unresolved-source</code>) <a class="header-anchor" href="#a-glob-matches-0-files-unresolved-source" aria-label="Permalink to &quot;A glob matches 0 files (`unresolved-source`)&quot;">​</a></h2><ul><li>Globs only work in the <strong>basename</strong> (last path segment): <code>swords/body_*.wav</code> is fine, <code>swords/*/*.wav</code> and <code>**/*.wav</code> are rejected as <code>invalid-source</code>.</li><li>The path is relative to the project <code>sounds/</code> folder. <code>swords/body_01.wav</code> means <code>MyProject/sounds/swords/body_01.wav</code> (your project folder). See <a href="./spec#sources">Sources</a>.</li><li>At resolve time, pass your <code>sounds/</code> listing so globs can expand; without it, patterns stay literal and warn <code>unresolved-source</code>. See <a href="./integration#pipeline">Integration: Pipeline</a>.</li></ul><h2 id="a-path-is-rejected-path-outside-sounds-invalid-source" tabindex="-1">A path is rejected (<code>path-outside-sounds</code> / <code>invalid-source</code>) <a class="header-anchor" href="#a-path-is-rejected-path-outside-sounds-invalid-source" aria-label="Permalink to &quot;A path is rejected (`path-outside-sounds` / `invalid-source`)&quot;">​</a></h2><p>Sources are hard <code>sounds/</code>-only and portable:</p><ul><li><code>path-outside-sounds</code> - you used a <code>/absolute</code> path or a leading <code>/</code>. Use a bare path like <code>swords/body_01.wav</code>.</li><li><code>invalid-source</code> - you used <code>..</code>, <code>**</code>, a directory glob (<code>*/</code>), or a backslash <code>\\</code>. None can escape the project root.</li></ul><p>The <code>sounds/</code> prefix alias is allowed and normalized: <code>sounds/swords/body_01.wav</code> is the same file as <code>swords/body_01.wav</code>.</p><h2 id="a-unit-error-blocks-the-plan" tabindex="-1">A unit error blocks the plan <a class="header-anchor" href="#a-unit-error-blocks-the-plan" aria-label="Permalink to &quot;A unit error blocks the plan&quot;">​</a></h2><ul><li><code>x-soundef-sameUnit</code> - a <code>randomRange</code> has different units on each end, e.g. <code>[-2dB, 5ms]</code>. Both ends must share the unit.</li><li><code>unit-mismatch</code> - the range unit does not match the field. <code>gain</code> wants <code>dB</code>, <code>offset</code> wants <code>ms</code>, <code>pitch</code> wants <code>st</code>.</li><li><code>invalid-unit</code> - a scalar string is not a recognized form like <code>-2dB</code>, <code>17ms</code>, <code>-2st</code>, <code>1.5s</code>.</li></ul><p>See <a href="./spec#units">Units</a> for the full rules.</p><h2 id="i-get-an-implicit-unit-warning" tabindex="-1">I get an <code>implicit-unit</code> warning <a class="header-anchor" href="#i-get-an-implicit-unit-warning" aria-label="Permalink to &quot;I get an `implicit-unit` warning&quot;">​</a></h2><p>You used a bare number where a unit is canonical, e.g. <code>gain: 1</code>. It <strong>still plays</strong> (as <code>1dB</code>), but the canonical form is <code>gain: 1dB</code>. This keeps old files working instead of failing. Add the unit to clear the warning.</p><h2 id="decode-fails-unsupported-format" tabindex="-1">Decode fails / <code>unsupported-format</code> <a class="header-anchor" href="#decode-fails-unsupported-format" aria-label="Permalink to &quot;Decode fails / `unsupported-format`&quot;">​</a></h2><ul><li>Any extension passes the schema, but Forge warns <code>unsupported-format</code> for files outside the supported set. First-class: <code>wav, ogg, oga, opus, mp3, flac, aif, aiff</code>; best-effort: <code>m4a, aac, webm</code>.</li><li>If a layer&#39;s file cannot be decoded, <strong>bake skips that layer with a warning</strong>. Convert the file to a supported format.</li></ul><h2 id="the-bake-clips-red-dot" tabindex="-1">The bake clips (red dot) <a class="header-anchor" href="#the-bake-clips-red-dot" aria-label="Permalink to &quot;The bake clips (red dot)&quot;">​</a></h2><ul><li>A persistent red clip dot on the final waveform + a <code>bake-clipped</code> warning means the render peaked over <code>0dB</code>. A hard limiter at <code>-0.1dB</code> prevents the file itself from clipping, but the mix is too hot.</li><li><strong>Fix:</strong> lower a layer&#39;s <code>gain</code> and re-bake, or add an <a href="./spec#effects"><code>effects.limiter</code></a> bus (see <a href="./examples#hammer_limited-the-limiter-bus"><code>hammer_limited</code></a>).</li><li><code>Normalize -1dB</code> scales the baked buffer explicitly and clears the dot.</li></ul><h2 id="external-edits-are-not-loading-dirty-banner" tabindex="-1">External edits are not loading (dirty banner) <a class="header-anchor" href="#external-edits-are-not-loading-dirty-banner" aria-label="Permalink to &quot;External edits are not loading (dirty banner)&quot;">​</a></h2><p>There is no auto-save. When another program writes the <code>.soundef</code> on disk:</p><ul><li>If your editor is <strong>clean</strong>, Forge reloads the file.</li><li>If your editor has <strong>unsaved changes</strong>, Forge shows a banner <code>changed on disk - [Load] [Keep mine]</code>. Preview keeps running from your editor until you choose. Select <code>Load</code> to use the disk file or <code>Keep mine</code> to keep your edits. See the <a href="./faq">FAQ</a>.</li></ul><h2 id="the-watcher-is-not-firing-at-all" tabindex="-1">The watcher is not firing at all <a class="header-anchor" href="#the-watcher-is-not-firing-at-all" aria-label="Permalink to &quot;The watcher is not firing at all&quot;">​</a></h2><p>The watcher runs on <code>sounds/*</code> and <code>soundefs/*</code> with a 120ms debounce, and only when a valid project is open. Confirm you opened your project folder (any name) that has both <code>sounds/</code> and <code>soundefs/</code> siblings (lowercase).</p><h2 id="version-errors-unknown-version-missing-required" tabindex="-1"><code>version</code> errors (<code>unknown-version</code> / <code>missing-required</code>) <a class="header-anchor" href="#version-errors-unknown-version-missing-required" aria-label="Permalink to &quot;`version` errors (`unknown-version` / `missing-required`)&quot;">​</a></h2><ul><li><code>version</code> must be exactly <code>0</code>. Any other value fails with <code>unknown-version</code>.</li><li><code>name</code>, <code>version</code>, and <code>layers</code> are all required - a missing one is <code>missing-required</code>.</li></ul><h2 id="unknown-property" tabindex="-1"><code>unknown-property</code> <a class="header-anchor" href="#unknown-property" aria-label="Permalink to &quot;`unknown-property`&quot;">​</a></h2><p>You used a key that is not allowed at the top level or inside a layer. The only place unknown keys are tolerated is <strong>inside <code>effects</code></strong>, where they surface as <code>forge-only</code> warnings (ignored live, baked only), not errors.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./spec#diagnostics">Spec: Diagnostics</a> - the complete table.</li><li><a href="./integration">Integration</a> - resolve a <code>PlaybackPlan</code> and map it to audio.</li><li><a href="./faq">FAQ</a> - offset vs delay, no auto-save, absolute paths.</li></ul>',30)])])}const f=o(r,[["render",i]]);export{p as __pageData,f as default};
